PORT=5001
CORS_ORIGIN=http://localhost:3000

# Optional: real SMTP (else Ethereal dev inbox is created automatically)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Where to send pharmacy notifications (test email)
PHARMACY_EMAIL=test-pharmacy@example.com
